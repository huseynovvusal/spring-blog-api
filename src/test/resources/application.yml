spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  mail:
    host: localhost
    port: 1025
    username: testuser
    password: testpass
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

security:
  jwt:
    secret_key: yQw1nQw8w6v7v8QkQk9pQk3vQk2pQk1nQk0pQk9nQk8pQk7vQk6pQk5nQk4pQk3v
    expiration_time: 3600000

client:
  app:
    url: http://localhost:3000

openapi:
  server:
    url: ${OPENAPI_SERVER_URL:http://localhost:8080/api/v1}
    description: ${OPENAPI_SERVER_DESCRIPTION:Test}
